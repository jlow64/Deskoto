/*
    Need to make each link section lead to respective urls
    Need to research what method we want to use to link to pages.
    i.e. via window
*/
const BreadCrumbs = () => {
  return <div>Path {'> > >'}</div>;
};

export default BreadCrumbs;
